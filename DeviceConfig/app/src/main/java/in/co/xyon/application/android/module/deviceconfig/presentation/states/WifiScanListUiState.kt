package `in`.co.xyon.application.android.module.deviceconfig.presentation.states

enum class WifiScanListUiState {
    LIST_LOADING,
    LIST_FOUND,
    LIST_FOUND_ERROR,
    DEVICE_DISCONNECTED,
    //SHOW_JOIN_NETWORK_DIALOG,
    IDLE_STATE
}